node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
next-env.d.ts
.vercel
tsconfig.tsbuildinfo
.lighthouse

# Working archives that live in the repo root. AGENTS.zip and public.zip are
# ~51 MB between them, and a zip committed once stays in git history forever
# even after it is deleted. Source of truth for anything inside them is the
# unzipped folder next to it.
*.zip
